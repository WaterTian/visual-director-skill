[project]
name = "visual-director-skill"
version = "0.5.0"
description = "Portable Visual Director contracts, source adapters, prompt compiler, and quality gates."
authors = [{ name = "WaterTian" }]
requires-python = ">=3.11"
dependencies = [
  "jsonschema>=4.23,<5",
  "pillow>=11,<13"
]

[tool.uv]
package = false
